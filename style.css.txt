* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --gold: #c7a45b;
    --gold-light: #e5c984;
    --gold-dark: #80551c;
    --royal: #25142d;
    --royal-light: #43254c;
    --dark: #120b16;
    --text: #342d38;
    --light: #f7f3ec;
    --white: #ffffff;
    --border: #e5dccd;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: "Inter", sans-serif;
    color: var(--text);
    background: #faf8f4;
}

body.urdu {
    font-family: "Noto Naskh Arabic", serif;
    direction: rtl;
}

.container {
    width: min(1150px, 92%);
    margin: auto;
}

.hidden {
    display: none !important;
}


/* LANGUAGE SCREEN */

.language-screen {
    position: fixed;
    inset: 0;
    background:
        radial-gradient(circle at 70% 20%, rgba(199,164,91,.25), transparent 25%),
        linear-gradient(135deg, #130b18, #3c2444);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
}

.language-box {
    width: min(500px, 90%);
    background: white;
    border-radius: 25px;
    padding: 50px 35px;
    text-align: center;
    box-shadow: 0 30px 80px rgba(0,0,0,.35);
}

.language-logo {
    font-size: 38px;
    font-weight: 800;
    color: var(--dark);
    margin-bottom: 25px;
}

.language-logo span {
    color: var(--gold);
}

.language-box h1 {
    font-size: 30px;
    margin-bottom: 10px;
    color: var(--dark);
}

.language-box p {
    color: #667085;
    margin-bottom: 30px;
}

.language-buttons {
    display: flex;
    gap: 15px;
    justify-content: center;
}

.language-btn {
    border: none;
    padding: 15px 35px;
    border-radius: 10px;
    font-size: 17px;
    cursor: pointer;
    font-weight: 700;
    transition: .3s;
}

.language-btn:hover {
    transform: translateY(-3px);
}

.english {
    background: linear-gradient(135deg, var(--gold-light), var(--gold-dark));
    color: white;
}

.urdu {
    background: var(--royal);
    color: white;
}


/* NAVBAR */

.navbar {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(255,255,255,.95);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid #e5d6bd;
}

.nav-container {
    height: 75px;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.logo {
    text-decoration: none;
    font-size: 28px;
    font-weight: 800;
    color: var(--dark);
}

.logo span {
    color: var(--gold);
}

nav {
    display: flex;
    gap: 28px;
}

nav a {
    text-decoration: none;
    color: var(--dark);
    font-weight: 600;
    transition: .2s;
}

nav a:hover {
    color: var(--gold);
}

.menu-btn {
    display: none;
    border: none;
    background: transparent;
    font-size: 25px;
    cursor: pointer;
}


/* HERO */

.hero {
    min-height: 680px;
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
    background:
        radial-gradient(circle at 78% 25%, rgba(210,173,100,.28), transparent 20%),
        radial-gradient(circle at 15% 85%, rgba(184,145,69,.18), transparent 28%),
        linear-gradient(125deg, #160d20, #33203d 52%, #171019);
}

.hero-overlay {
    position: absolute;
    inset: 0;
    background:
        radial-gradient(circle at 20% 30%, rgba(255,255,255,.06), transparent 25%),
        linear-gradient(90deg, rgba(0,0,0,.25), transparent 70%);
}

.hero-content {
    position: relative;
    z-index: 2;
}

.hero-text {
    max-width: 760px;
    color: white;
}

.badge {
    display: inline-block;
    color: #f7dfad;
    background: rgba(184,145,69,.13);
    border: 1px solid rgba(214,178,109,.6);
    padding: 9px 16px;
    border-radius: 30px;
    margin-bottom: 20px;
    font-weight: 700;
    letter-spacing: .08em;
    font-size: 11px;
}

.hero h1 {
    font-size: clamp(42px, 6vw, 72px);
    line-height: 1.05;
    margin-bottom: 25px;
    letter-spacing: -.04em;
}

.hero p {
    font-size: 18px;
    line-height: 1.8;
    max-width: 650px;
    margin-bottom: 30px;
    color: #eee7f0;
}

.hero-buttons {
    display: flex;
    gap: 15px;
    flex-wrap: wrap;
}

.btn {
    display: inline-block;
    text-decoration: none;
    padding: 15px 25px;
    border-radius: 10px;
    font-weight: 700;
    cursor: pointer;
    transition: .3s;
    border: none;
}

.primary-btn {
    background: linear-gradient(135deg, #d4b26d, #9a6b25);
    color: white;
    box-shadow: 0 12px 30px rgba(105,70,20,.25);
}

.primary-btn:hover {
    transform: translateY(-3px);
}

.outline-btn {
    color: #f4dfb2;
    border: 2px solid #d4b26d;
}

.outline-btn:hover {
    background: #d4b26d;
    color: #25152c;
}


/* FEATURES */

.features {
    padding: 60px 0;
    background: linear-gradient(#fff, #faf6ee);
}

.feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
}

.feature {
    text-align: center;
    padding: 32px;
    border: 1px solid #e3d5bd;
    background: white;
    border-radius: 18px;
    transition: .3s;
}

.feature:hover {
    transform: translateY(-6px);
    box-shadow: 0 18px 45px rgba(66,40,25,.11);
}

.feature-icon {
    font-size: 42px;
    margin-bottom: 15px;
}

.feature h3 {
    margin-bottom: 8px;
    color: var(--dark);
}

.feature p {
    color: #667085;
    line-height: 1.6;
}


/* SECTIONS */

.products-section,
.reviews-section {
    padding: 100px 0;
    background: #f5f0e8;
}

.section-heading {
    text-align: center;
    margin-bottom: 50px;
}

.section-label {
    color: var(--gold);
    font-size: 13px;
    font-weight: 800;
    letter-spacing: .12em;
}

.section-heading h2,
.about-content h2,
.contact-section h2 {
    color: var(--dark);
    font-size: clamp(30px, 4vw, 45px);
    margin: 12px 0;
}

.section-heading p {
    color: #667085;
}


/* PRODUCT SEARCH */

.product-tools {
    display: flex;
    gap: 15px;
    margin-bottom: 35px;
}

.product-tools input {
    width: 100%;
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 15px;
    font-size: 16px;
    outline: none;
    background: white;
}

.product-tools input:focus {
    border-color: var(--gold);
    box-shadow: 0 0 0 3px rgba(184,145,69,.12);
}


/* PRODUCTS */

.products-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
}

.product-card {
    background: white;
    border-radius: 18px;
    overflow: hidden;
    border: 1px solid #e4d8c5;
    transition: .3s;
}

.product-card:hover {
    transform: translateY(-7px);
    box-shadow: 0 20px 45px rgba(50,31,21,.16);
}

.product-color {
    height: 210px;
    position: relative;
}

.product-color::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,.2), transparent 60%);
}

.product-info {
    padding: 22px;
}

.product-category {
    color: var(--gold);
    font-size: 12px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .12em;
}

.product-info h3 {
    color: var(--dark);
    font-size: 21px;
    margin: 8px 0;
}

.product-info p {
    color: #667085;
    line-height: 1.6;
    font-size: 14px;
    margin-bottom: 15px;
}

.product-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
}

.rating {
    color: #b8842e;
    font-size: 14px;
}

.view-btn {
    background: linear-gradient(135deg, #bd984d, #80551c);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 10px 15px;
    cursor: pointer;
}


/* ABOUT */

.about-section {
    padding: 100px 0;
    background: #fffaf3;
}

.about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 70px;
    align-items: center;
}

.about-image {
    height: 450px;
    border-radius: 30px;
    background:
        radial-gradient(circle at 18% 15%, rgba(207,166,86,.32), transparent 25%),
        linear-gradient(145deg, #1a0e20, #412846);
    display: flex;
    align-items: center;
    justify-content: center;
}

.hair-visual {
    width: 76%;
    min-height: 72%;
    border: 1px solid rgba(232,200,137,.7);
    color: white;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
    box-shadow: inset 0 0 50px rgba(207,166,86,.08);
}

.hair-visual::before,
.hair-visual::after {
    content: "";
    position: absolute;
    width: 55px;
    height: 55px;
    border: 1px solid #e8c889;
}

.hair-visual::before {
    top: -1px;
    left: -1px;
    border-right: 0;
    border-bottom: 0;
}

.hair-visual::after {
    bottom: -1px;
    right: -1px;
    border-left: 0;
    border-top: 0;
}

.hair-visual-label,
.hair-visual-sub {
    font-size: 10px;
    letter-spacing: .3em;
    color: #e5c477;
}

.hair-visual strong {
    font-size: clamp(48px, 7vw, 82px);
    line-height: .8;
    letter-spacing: -.07em;
    margin: 25px 0;
    color: white;
}

.hair-visual em {
    font-style: normal;
    font-weight: 300;
    letter-spacing: .12em;
}

.about-content p {
    line-height: 1.8;
    margin: 18px 0;
    color: #667085;
}

.stats {
    display: flex;
    gap: 45px;
    margin-top: 30px;
}

.stats div {
    display: flex;
    flex-direction: column;
}

.stats strong {
    color: var(--gold);
    font-size: 30px;
}

.stats span {
    color: #667085;
}


/* DISTRIBUTOR */

.distributor-section {
    padding: 70px 0;
    color: white;
    background:
        radial-gradient(circle at 85% 50%, rgba(185,143,65,.22), transparent 30%),
        linear-gradient(120deg, #170d1d, #35213b);
}

.distributor-box {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 30px;
}

.distributor-box h2 {
    font-size: clamp(30px, 4vw, 48px);
    margin: 10px 0;
}

.distributor-box p {
    color: #e0d6e3;
    max-width: 650px;
    line-height: 1.7;
}

.section-label.light {
    color: #e8c577;
}

.whatsapp-btn {
    flex-shrink: 0;
    border: none;
    background: linear-gradient(135deg, #bd984d, #80551c);
    color: white;
    padding: 16px 23px;
    border-radius: 10px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    transition: .3s;
}

.whatsapp-btn:hover {
    transform: translateY(-3px);
}


/* REVIEWS */

.reviews-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
}

.review-card {
    background: white;
    padding: 30px;
    border-radius: 18px;
    border: 1px solid #e6dac7;
}

.stars {
    color: #b8842e;
    font-size: 20px;
    margin-bottom: 18px;
}

.review-card p {
    color: #475467;
    line-height: 1.8;
    min-height: 85px;
}

.review-person {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 20px;
}

.avatar {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    background: linear-gradient(135deg, #c9a35a, #80531d);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
}

.review-person strong,
.review-person span {
    display: block;
}

.review-person span {
    color: #98a2b3;
    font-size: 13px;
}


/* CONTACT */

.contact-section {
    padding: 100px 0;
    background: white;
}

.contact-grid {
    text-align: center;
}

.contact-section p {
    max-width: 600px;
    margin: auto;
    color: #667085;
    line-height: 1.8;
}

.contact-whatsapp {
    margin-top: 25px;
    background: linear-gradient(135deg, #bd984d, #80551c);
    color: white;
    border: none;
    padding: 15px 25px;
    border-radius: 10px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
}


/* FOOTER */

footer {
    background: linear-gradient(100deg, #100914, #28182c);
    color: #c5b8c8;
    padding: 30px 0;
}

.footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.footer-logo {
    color: white;
    font-size: 25px;
    font-weight: 800;
}

.footer-logo span {
    color: #d4b26d;
}


/* MODAL */

.modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 5000;
    background: rgba(0,0,0,.7);
    justify-content: center;
    align-items: center;
    padding: 20px;
}

.modal.show {
    display: flex;
}

.modal-content {
    background: white;
    width: min(550px, 100%);
    border-radius: 20px;
    padding: 30px;
    position: relative;
    max-height: 90vh;
    overflow-y: auto;
    border: 1px solid #eadcc7;
}

.close-modal {
    position: absolute;
    top: 12px;
    right: 15px;
    border: none;
    background: #f2f0eb;
    width: 35px;
    height: 35px;
    border-radius: 50%;
    font-size: 22px;
    cursor: pointer;
}

.modal-product-color {
    height: 220px;
    border-radius: 15px;
    margin-bottom: 20px;
}

.modal-content h2 {
    color: var(--dark);
    margin-bottom: 10px;
}

.modal-content p {
    line-height: 1.7;
    color: #667085;
}

.modal-rating {
    margin: 15px 0;
    color: #b8842e;
}


/* WHATSAPP FLOAT */

.whatsapp-float {
    position: fixed;
    right: 22px;
    bottom: 22px;
    width: 58px;
    height: 58px;
    border-radius: 50%;
    background: linear-gradient(135deg, #bd984d, #80551c);
    color: white;
    border: none;
    font-size: 27px;
    cursor: pointer;
    z-index: 2000;
    box-shadow: 0 10px 30px rgba(0,0,0,.25);
}


/* MOBILE */

@media (max-width: 800px) {

    nav {
        display: none;
        position: absolute;
        top: 75px;
        left: 0;
        right: 0;
        background: white;
        padding: 20px;
        flex-direction: column;
        box-shadow: 0 15px 30px rgba(0,0,0,.1);
    }

    nav.open {
        display: flex;
    }

    .menu-btn {
        display: block;
    }

    .feature-grid,
    .products-grid,
    .reviews-grid {
        grid-template-columns: 1fr;
    }

    .about-grid {
        grid-template-columns: 1fr;
        gap: 40px;
    }

    .about-image {
        height: 350px;
    }

    .distributor-box {
        flex-direction: column;
        align-items: flex-start;
    }

    .footer-content {
        flex-direction: column;
        gap: 10px;
        text-align: center;
    }

    .hero {
        min-height: 600px;
    }

    .hero h1 {
        font-size: 44px;
    }

    .stats {
        gap: 20px;
    }
}